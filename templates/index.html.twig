
   {% extends 'base.html.twig' %}

   {% block title %}Bienvenue sur Marmi-Wild-Séries!{% endblock %}
   {% block body %}
   <h1>Bienvenue !</h1>

   <a class="btn btn-primary m-0" href="{{ path('category_index') }}">Liste des catégories</a>
<br><br>
   <a class="btn btn-primary m-0" href="{{ path('program_index') }}">Liste des programmes</a>
<br><br>
   <a class="btn btn-primary m-0" href="{{ path('program_new') }}">Ajouter un programme</a>
<br><br>
   <a class="btn btn-primary m-0" href="{{ path('category_new') }}">Ajouter une catégorie</a>
<br><br>
   <a class="btn btn-primary m-0" href="{{ path('season_new') }}">Ajouter une saison</a>
<br><br>
   <a class="btn btn-primary m-0" href="{{ path('episode_new') }}">Ajouter un episode</a>
   {% endblock %}
