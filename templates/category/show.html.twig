{% extends 'base.html.twig' %}

{% block title %}Catégorie : {{ category.name }}{% endblock %}

{% block body %}
    <div class="media">
        <div class="media-body">
            <h1 class="mt-0">Séries de la catégorie: {{ category.name }}</h1>
        </div>
        {% for program in programs %}
        <div>
            <h2>{{ program.id }} / {{ program.title }}</h2>
            <p>{{ program.synopsis }}</p>
            <a class="btn btn-primary m-0" href="{{ path("program_show", { slug: program.slug }) }}">Voir la fiche</a>
        </div>
    {% else %}
        Aucune série trouvée.
    {% endfor %}
    </div>

    <a class="btn btn-secondary m-0" href="{{ path('category_index') }}">
        Retour à la liste des catégories
    </a>
{% endblock %}